function * {{camelCase sagaName}} (api, action) {
  {{#if isPromisified}}
  const { payload, resolve, reject } = action
  {{else}}
  const { payload } = action
  {{/if}}
  const headers = yield call(getAuthHeaders)
  const response = yield call(api.{{camelCase reduxActionName}}, headers)
  if (response.ok) {
    const data = get(response, 'data')
    yield put(Actions.{{camelCase reduxActionName}}Success())
    {{#if isPromisified}}
    return resolve()
    {{/if}}
  } else {
    yield put(Actions.{{camelCase reduxActionName}}Failure(parseError(response)))
    {{#if isPromisified}}
    return reject(error)
    {{/if}}
  }
}

// ADD_SAGA_ACTION
