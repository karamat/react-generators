import { StyleSheet } from 'react-native'
import { Colors, Metrics } from '../../../Themes/'

export default StyleSheet.create({
  screenBody: {
    padding: 15,
    flexDirection: 'column',
    minHeight: Metrics.screenHeight - 160,
    justifyContent: 'flex-start'
  },
  nextButton: {
  },
  nextButtonText: {
    width: '100%',
    color: 'white',
  },
  bodyTop: {
    flex: 30,
  },
  bodyBottom: {
    flex: 1,
    marginBottom: -20,
  }
})
